最初にページを表示した際、画像がスムーズに表示されるようにしたいです。このために以下の機能を実装してください。

- graph.py において、9分毎に下記のグラフを作成し、グラフを作成に使った start と end とともにキャッシュしておく
  start は現在時刻の7日前 (一番古いデータの日時よりも古くなる場合は、一番古いデータの日時上書きする。キャッシュする start の値も)
  end は現在時刻

- graph API は use_cache というパラメータを受け取れるようにし、これが指定され、かつ値が0以外だった場合、
  指定された日時が近い同じグラフのキャッシュデータがあれば、それを返すようにする。
  日時が近いことは、start と end ともに、キャッシュデータのそれとの時間差が10分未満であることで判定する。

- react は初期状態でグラフをリクエストする際、use_cache=1 をつけてリクエストを行う。それ以外の場合は、use_cache は指定しない。
